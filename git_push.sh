#!/bin/bash

# Gitリポジトリの最新状態を取得
git pull

# すべての新規・変更されたファイルをステージに追加
git add -A

# コミットメッセージを引数として受け取る
if [ -z "$1" ]; then
    echo "コミットメッセージを指定してください。"
    exit 1
fi

# 引数として受け取ったメッセージでコミット
git commit -m "$1"

# 変更をリモートリポジトリにプッシュ
git push

